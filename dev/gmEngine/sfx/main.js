import sfx from './sfx.js';

/*
  Methods:

  sfx.toneStart(code, volume, wave);
  sfx.toneEnd(code);
  sfx.noiseStart(id, freq, volume, wave);
  sfx.noiseModify(id, freq, volume, wave);
  sfx.noiseStop(id);
  


*/
